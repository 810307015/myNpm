/**
 * 微信小程序
 */
import { HttpClient, ZRequest } from "./request";

export default class WechatHttpClient extends HttpClient {
    constructor() {
        super();
    }

    request(config: ZRequest) {
        
    }
}